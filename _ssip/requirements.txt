mace-torch>=0.3.6       # MACE-OFF23 geometry optimisation
# aimnet2calc is NOT on PyPI — two options:
#   Option A (preferred): pip install git+https://github.com/isayevlab/AIMNet2.git
#   Option B: omit; charge_predict.py falls back to torch.hub automatically
ase>=3.23               # ASE Atoms object + LBFGS optimiser
rdkit>=2024.03          # 3D init (ETKDG), SMARTS, vdW radii
numpy
scipy                   # Shrake-Rupley surface, calibration fitting
pandas
tqdm
torch>=2.0              # required by mace-torch and aimnet2calc
